# Benchmarking Lens

Audit baseline relevance, implementation fidelity, tuning and compute budgets, data access parity, metric choice, uncertainty, statistical comparison, ablations, and failure analysis. Check whether comparisons use identical splits and preprocessing and whether simple baselines are present. Avoid declaring a method superior from a single favorable metric or dataset.
