# Multi-Omics Lens

Audit whether modalities come from matched biological units, how unmatched or missing modalities are handled, scale and identifier compatibility, integration objectives, modality dominance, latent-space validation, circular supervision, and modality-specific versus joint baselines. Require evidence that integration preserves useful biology instead of only improving mixing.
